<Window
    x:Class="PrimeDocs_Desktop.UI.Configurations"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:components="clr-namespace:PrimeDocs_Desktop.UI.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PrimeDocs_Desktop.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Configurations"
    Width="600"
    Height="450"
    MinWidth="600"
    MinHeight="800"
    AllowsTransparency="True"
    Background="Transparent"
    Icon="/Resources/Icons/PrimeDocs.png"
    WindowStyle="None"
    mc:Ignorable="d" ResizeMode="NoResize">
    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="0"
            GlassFrameThickness="0"
            ResizeBorderThickness="7"
            UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>
    <Border
        x:Name="MainBorder"
        Background="White"
        BorderBrush="#001BC9"
        BorderThickness="2"

        CornerRadius="13">
        <Grid>
            <Border
                x:Name="SecondaryBorder"
                Background="White"
                CornerRadius="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <components:TopBarPrimeDocsClose
                        x:Name="tbpInitialWindowTopBar"
                        Height="35"
                        VerticalAlignment="Center" HorizontalAlignment="Center" Width="596" />
                    <ScrollViewer
                        x:Name="scrollViewer"
                        Grid.Row="1"
                        Padding="0"
                        HorizontalScrollBarVisibility="Disabled"
                        VerticalScrollBarVisibility="Auto" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
